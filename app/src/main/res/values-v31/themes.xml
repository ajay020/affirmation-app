<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.AffirmWell" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:windowSplashScreenBackground">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/purple_700</item>
        <item name="android:windowSplashScreenAnimationDuration">300</item>
<!--        <item name="postSplashScreenTheme">@style/Theme.AffirmWell.NoActionBar</item>-->
    </style>
</resources>
